package org.example.service.checkings;

import org.example.model.Matrix;

public interface MatrixCheckings {

    boolean check(final Matrix firstMatrix,
                  final Matrix secondMatrix);
}
